#Sample code
class sample:
    def __init__(self): #constructor(method) - self is used to call the constructor method.
        print("Hello")
obj=sample() #if not called not print

Constructor is no need of calling, it automatically executes when a function gets executed.


def-  outside the class it is function
    -inside the class it is method

def__init__(self):
It is a special method in a python we call it as constructor.
In python we have only one constructor.

For a single class we can create any number of objects.

Mainly, we will keep the code inside the constructor, whose control we dont wanted to give it to the user.
